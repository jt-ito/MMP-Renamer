{
  "name": "mmp-renamer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd web && npm run dev\"",
    "web:install": "cd web && npm install",
    "install:all": "npm install && cd web && npm install"
  ,"test:unit": "node ./scripts/tests/enrich-path.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
  "uuid": "^9.0.0",
  "bcryptjs": "^2.4.3",
  "cookie-session": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
